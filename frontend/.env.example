# URL base del backend FastAPI. In sviluppo punta a localhost; in produzione
# va impostato all'URL pubblico del backend (es. tramite variabili d'ambiente
# del provider di hosting).
VITE_API_BASE=http://localhost:8000
